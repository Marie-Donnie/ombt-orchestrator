- name: Start cockroachdb
  docker_container:
    name: "cockroachdb"
    image: "cockroachdb/cockroach"
    hostname: "{{ inventory_hostname }}"
    detach: True
    ports: 26257
    command: "cockroachdb/cockroach:v1.1.4 start --insecure"
